/**
 * UI Constants
 * Centralized constants for UI components to maintain consistency
 */

// Tooltip Configuration
export const TOOLTIP_DELAY_MS = 300;

// Search Configuration
export const SEARCH_FUZZY_THRESHOLD = 0.3;
export const SEARCH_IGNORE_LOCATION = true;

// Layout & Sizing
export const MAX_SEARCH_RESULTS_HEIGHT = 'max-h-48';
export const MAX_RUN_SELECTOR_HEIGHT = 'max-h-40';

// Avatar Sizes
export const AVATAR_SIZE_CLASSES = {
  sm: 'w-12 h-12 text-base',
  md: 'w-16 h-16 text-xl',
  lg: 'w-20 h-20 text-2xl'
};

// Avatar Default Gradients
export const AVATAR_DEFAULT_GRADIENT = {
  from: 'from-green-500',
  to: 'to-teal-600'
};

export const AVATAR_STUDY_GRADIENT = {
  from: 'from-blue-500',
  to: 'to-purple-600'
};

// Text Truncation
export const LINE_CLAMP_DEFAULT = 'line-clamp-2';
export const LINE_CLAMP_SINGLE = 'line-clamp-1';

// Minimum Heights
export const MIN_HEIGHT_EMPTY_STATE = 'min-h-[220px]';
