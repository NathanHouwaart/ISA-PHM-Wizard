===================================================================================
IMPLEMENTATION PLAN: Split Study Variable Slides into Fault Specs & Operating Conditions
===================================================================================

PROJECT CONTEXT:
- ISA-PHM Wizard - React + Vite application for managing study metadata
- Uses GlobalDataContext for state management with localStorage persistence
- Carousel-style slides navigation (index-based)
- Pattern: Simple View (cards) + Grid View (DataGrid) for most entity slides

===================================================================================
CURRENT STATE ANALYSIS
===================================================================================

EXISTING FILES TO MODIFY/SPLIT:
1. StudyVariableDefinitionSlide.jsx (index 5 in slides array)
   - Currently handles ALL study variables (both fault specs and operating conditions)
   - Has simple view (Collection + cards) and grid view (DataGrid)
   - Uses VARIABLE_TYPE_OPTIONS dropdown with 6 options including "Operating condition"
   
2. StudyVariableSlide.jsx (index 6 in slides array) - MAPPINGS
   - Maps variables to study runs
   - Shows all variables together (currently no grouping by type)
   - Needs to show BOTH types grouped separately

3. slides.js
   - Exports array of slide components
   - Current order: Introduction, Investigation, Contact, Publication, Study, 
     StudyVariableDefinition (5), StudyVariableMapping (6), MeasurementProtocol...

SUPPORTING FILES:
- StudyVariableForm.jsx - Form for editing variables (has type dropdown)
- StudyVariableCard.jsx - Card component for displaying variables
- useVariables.jsx - Hook for variable CRUD operations
- variableTypes.jsx - Defines VARIABLE_TYPE_OPTIONS constant
- GlobalDataContext.jsx - State: studyVariables array, setStudyVariables

CURRENT VARIABLE TYPES (variableTypes.jsx):
export const VARIABLE_TYPE_OPTIONS = [
    'Qualitative fault specification', 
    'Quantitative fault specification', 
    'Operating condition',
    'Time',
    'Damage',
    'Other'
];

===================================================================================
REQUIREMENTS BREAKDOWN
===================================================================================

TARGET STATE:
1. TWO SEPARATE SLIDES for definitions:
   Slide A: "Fault Specifications" (index 5)
   Slide B: "Operating Conditions" (index 6)
   
2. ONE SLIDE for mappings (now at index 7):
   "Study Variable Mappings" - shows BOTH types grouped

SPECIFIC REQUIREMENTS:

FOR FAULT SPECIFICATIONS SLIDE:
- Filename: FaultSpecificationsSlide.jsx
- Title: "Fault Specifications"
- Simple View: Collection + cards (useVariables hook filtered)
- Grid View: DataGrid (filtered data)
- Type field: Dropdown WITHOUT "Operating condition" option
- Data source: studyVariables filtered by type !== 'Operating condition'
- Allowed types: ['Qualitative fault specification', 'Quantitative fault specification', 'Time', 'Damage', 'Other']

FOR OPERATING CONDITIONS SLIDE:
- Filename: OperatingConditionsSlide.jsx
- Title: "Operating Conditions"
- Simple View: Collection + cards (useVariables hook filtered)
- Grid View: DataGrid (filtered data)
- Type field: FIXED to 'Operating condition' (NOT a dropdown - maybe hidden or readonly text)
- Data source: studyVariables filtered by type === 'Operating condition'

FOR MAPPINGS SLIDE (StudyVariableSlide.jsx):
- Keep existing filename and functionality
- Add grouping/sectioning by type:
  * Section 1: Fault Specifications (all non-operating-condition variables)
  * Section 2: Operating Conditions (operating condition variables)
- Both simple view and grid view should show grouping
- User can see complete test matrix with both types visible

===================================================================================
KEY QUESTIONS FOR USER
===================================================================================

DATA MANAGEMENT:
Q1: Should we keep ONE unified studyVariables array in GlobalDataContext?
    OR split into two arrays: faultSpecifications + operatingConditions?
    
    RECOMMENDATION: Keep ONE array with type-based filtering. Easier for mappings,
    simpler for data export, no migration needed.

Q2: When creating a new fault specification, what should be the default type?
    Currently defaults to VARIABLE_TYPE_OPTIONS[0] = 'Qualitative fault specification'
    Keep this default?

Q3: When creating a new operating condition, should type field be:
    A) Hidden completely (user never sees it)
    B) Shown as readonly text "Operating condition"
    C) Shown but disabled with fixed value
    
    RECOMMENDATION: Hidden completely - simpler UX

COMPONENT REUSE:
Q4: Can we reuse the same Card component (StudyVariableCard) for both slides?
    Or do you want different visual styling for fault specs vs operating conditions?
    
    RECOMMENDATION: Reuse - maintains consistency

Q5: Can we reuse the same Form component for both?
    - Fault specs form: Show type dropdown (filtered options)
    - Operating conditions form: Hide type field or make it readonly
    
    OR create two separate forms?
    
    RECOMMENDATION: Reuse with conditional rendering based on a prop

HOOKS STRATEGY:
Q6: Should we create new hooks?
    Option A: Create useFaultSpecifications() and useOperatingConditions() that filter studyVariables
    Option B: Extend useVariables() to accept a filter parameter: useVariables({ typeFilter: ... })
    
    RECOMMENDATION: Option B - more flexible, less code duplication

NAVIGATION & ROUTING:
Q7: Slide order - where should Operating Conditions go?
    Current: [0-4], StudyVariableDef(5), StudyVariableMapping(6), [7-10]
    Proposed: [0-4], FaultSpecs(5), OperatingConditions(6), StudyVariableMapping(7), [8-11]
    
    Is this correct?

Q8: Should the FIRST_WIDE_SLIDE_INDEX in IsaQuestionnaire.jsx change?
    Currently set to 6 (was StudyVariableSlide)
    New index would be 7 (StudyVariableSlide moved)
    Need to update this?

MAPPINGS SLIDE GROUPING:
Q9: For the mappings slide grouping, do you want:
    A) Two separate grids stacked vertically (one for fault specs, one for operating conditions)
    B) One unified grid with visual section headers/dividers
    C) Tabs to switch between fault specs and operating conditions
    
    RECOMMENDATION: Option A (two grids stacked) - clearest separation, consistent with 
    existing pattern in the codebase (see multi-study grids in StudyVariableSlide)

Q10: In simple view of mappings, how should grouping work?
     Currently uses StudyVariableMappingPanel component.
     Should it show fault specs first, then operating conditions, with section headers?

FORM FIELD BEHAVIOR:
Q11: In grid view, should the type column be:
     - For FaultSpecificationsSlide: Editable dropdown with filtered options?
     - For OperatingConditionsSlide: Hidden completely OR readonly?
     
     RECOMMENDATION: 
     - Fault specs: Editable dropdown
     - Operating conditions: Hidden (remove from columns array)

===================================================================================
PROPOSED IMPLEMENTATION STEPS
===================================================================================

PHASE 1: PREPARE UTILITIES & HOOKS
□ Create filter utilities in variableTypes.jsx:
  - FAULT_SPEC_TYPES array
  - OPERATING_CONDITION_TYPE constant
  - isFaultSpecification(variable)
  - isOperatingCondition(variable)
  
□ Update useVariables hook to support filtering:
  - Add optional typeFilter parameter
  - Return filtered items based on type

PHASE 2: CREATE NEW SLIDE COMPONENTS
□ Create FaultSpecificationsSlide.jsx:
  - Copy StudyVariableDefinitionSlide.jsx structure
  - Filter data: studyVariables.filter(isFaultSpecification)
  - Update grid config: type dropdown source = FAULT_SPEC_TYPES
  - Update Collection to use filtered hook
  - Update titles, subtitles, displayName
  
□ Create OperatingConditionsSlide.jsx:
  - Copy StudyVariableDefinitionSlide.jsx structure
  - Filter data: studyVariables.filter(isOperatingCondition)
  - Remove type column from grid OR make readonly
  - Update form to hide/fix type field
  - Update titles, subtitles, displayName

PHASE 3: UPDATE MAPPINGS SLIDE
□ Modify StudyVariableSlide.jsx:
  - Split studyVariables into faultSpecs and operatingConditions arrays
  - Grid view: Create two separate grid sections with headers
  - Simple view: Update StudyVariableMappingPanel to accept grouped data
  - Add visual section headers/dividers

PHASE 4: UPDATE NAVIGATION & ROUTING
□ Update slides.js:
  - Import new slides
  - Replace StudyVariableDefinitionSlide with:
    * FaultSpecificationsSlide (index 5)
    * OperatingConditionsSlide (index 6)
  - StudyVariableSlide moves to index 7
  
□ Update IsaQuestionnaire.jsx:
  - FIRST_WIDE_SLIDE_INDEX = 7 (was 6)
  - Verify tab state logic works correctly

PHASE 5: UPDATE FORMS (IF NEEDED)
□ If reusing form:
  - Add prop to StudyVariableForm: variableCategory='fault-spec' | 'operating-condition'
  - Conditionally render type field based on category
  - Set default type based on category

□ OR create separate forms:
  - FaultSpecificationForm.jsx
  - OperatingConditionForm.jsx

PHASE 6: TESTING & VALIDATION
□ Test adding/editing fault specifications
□ Test adding/editing operating conditions
□ Test mappings with both types
□ Test navigation between new slides
□ Test localStorage persistence
□ Test grid view undo/redo
□ Test simple/grid view tab switching

===================================================================================
REUSABLE PATTERNS FROM CODEBASE
===================================================================================

SLIDE STRUCTURE PATTERN:
const [ComponentName]Slide = forwardRef(({ onHeightChange, currentPage, pageIndex }, ref) => {
    const resizeRef = useResizeObserver(onHeightChange);
    const combinedRef = useCombinedRefs(ref, resizeRef);
    const { studyVariables, setStudyVariables } = useGlobalDataContext();
    const [selectedTab, setSelectedTab] = usePageTab(pageIndex, 'simple-view');
    
    // Grid config with staticColumns, customActions
    // TabSwitcher with simple-view and grid-view TabPanels
    // Simple view: Collection with cards
    // Grid view: DataGrid component
});

COLLECTION PATTERN:
<Collection onHeightChange={() => {}} itemHook={useVariables} grid={true}>
    <CollectionTitle>Title ({count})</CollectionTitle>
    <CollectionSubtitle>Subtitle</CollectionSubtitle>
    <CollectionAddButtonText>Add Button Text</CollectionAddButtonText>
</Collection>

GRID CONFIG PATTERN:
const gridConfig = useMemo(() => ({
    title: 'Grid Title',
    rowData: dataArray,
    columnData: [],
    mappings: [],
    staticColumns: [
        { prop: 'actions', cellTemplate: DeleteRowCellTemplate, ... },
        { prop: 'name', cellTemplate: BoldCell, pin: 'colPinStart', ... },
        { prop: 'type', columnType: 'select', source: options, ... }
    ],
    customActions: [{ label: '+ Add', onClick: handler, ... }]
}), [dependencies]);

HOOK PATTERN:
export const useVariables = () => {
    const { studyVariables, setStudyVariables } = useGlobalDataContext();
    
    const updateItem = (id, updates) => { ... };
    const addItem = (newItem) => { ... };
    const removeItem = (id) => { ... };
    
    return {
        items: studyVariables,
        updateItem,
        addItem,
        removeItem,
        cardComponent: StudyVariableCard,
        formComponent: StudyVariableForm
    };
};

===================================================================================
AI AGENT PROMPT (Ready to Use After Answering Questions)
===================================================================================

PROMPT FOR AI AGENT:

You are implementing a feature to split the Study Variable Definitions slide into two separate slides: 
one for Fault Specifications and one for Operating Conditions.

CONTEXT:
- This is a React + Vite app using GlobalDataContext for state management
- Current studyVariables array contains both fault specifications and operating conditions
- They are distinguished by the 'type' field
- Operating conditions have type === 'Operating condition'
- Fault specifications have type === anything else

REQUIREMENTS:
1. Create FaultSpecificationsSlide.jsx (copy from StudyVariableDefinitionSlide.jsx)
   - Filter to show only variables where type !== 'Operating condition'
   - Type dropdown should exclude 'Operating condition' option
   - Both simple view (cards) and grid view (DataGrid)

2. Create OperatingConditionsSlide.jsx (copy from StudyVariableDefinitionSlide.jsx)
   - Filter to show only variables where type === 'Operating condition'
   - Type field should be FIXED to 'Operating condition' (hidden from user)
   - Both simple view (cards) and grid view (DataGrid)

3. Update StudyVariableSlide.jsx (mappings)
   - Group variables by type when displaying
   - Show fault specifications section first
   - Show operating conditions section second
   - Both sections visible in both simple and grid views

4. Update slides.js
   - Import new slide components
   - Insert slides at correct indices
   - New order: [0-4], FaultSpecs(5), OpCond(6), Mappings(7), [8-11]

5. Update IsaQuestionnaire.jsx
   - Change FIRST_WIDE_SLIDE_INDEX from 6 to 7

6. Create utility functions in variableTypes.jsx:
   - Export FAULT_SPEC_TYPES array (all types except Operating condition)
   - Export isFaultSpecification(variable) helper
   - Export isOperatingCondition(variable) helper

PATTERNS TO FOLLOW:
- Reuse existing components: StudyVariableCard, StudyVariableForm, Collection, DataGrid
- Use same hook pattern: useVariables or create filtered versions
- Maintain simple/grid view tab switching pattern
- Keep localStorage auto-save (no changes needed)
- Follow existing slide structure with forwardRef, useResizeObserver, useCombinedRefs

FILES TO CREATE:
- src/components/Slides/FaultSpecificationsSlide.jsx
- src/components/Slides/OperatingConditionsSlide.jsx

FILES TO MODIFY:
- src/components/Slides/StudyVariableSlide.jsx (add grouping)
- src/components/Slides/slides.js (import and arrange)
- src/pages/IsaQuestionnaire.jsx (update FIRST_WIDE_SLIDE_INDEX)
- src/constants/variableTypes.jsx (add filter constants/helpers)
- src/hooks/useVariables.jsx (add filtering capability)
- src/components/StudyVariable/StudyVariableForm.jsx (conditional type field)

IMPLEMENTATION APPROACH:
[ANSWER BASED ON USER'S RESPONSES TO QUESTIONS Q1-Q11 ABOVE]

Start with Phase 1 (utilities), then Phase 2 (new slides), then remaining phases.
Test each phase before proceeding to the next.

===================================================================================
END OF IMPLEMENTATION PLAN
===================================================================================
